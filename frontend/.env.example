# URL base da API (sem /api no final — o api.js já adiciona)
# Em desenvolvimento, deixe em branco; o proxy do Vite redireciona /api → localhost:3001
# Em produção no Render, preencha com a URL do backend
VITE_API_URL=https://crm-funil-api.onrender.com/api
